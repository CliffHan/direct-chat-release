---
layout: default
permalink: /faq/
lang: zh-CN
lang_alt: /en/faq/
title: 常见问题 · direct-chat/isle
description: direct-chat / isle 常见问题：设备与身份、保持在线、加密与隐私、与其他软件的区别等。
---

<section class="section">
<div class="container narrow" markdown="1">

## 常见问题

这里收集关于 direct-chat 和 isle 的常见问题。没找到答案？直接写信给 <cliff@clifftop.win>。

<h3 class="faq-group">概念</h3>

<h3 class="faq-q">设备和身份是什么关系？</h3>

每台设备最多绑定一个身份；同一个身份可以被多台设备共用。

设备与身份的关系是这样建立的：

1. 设备初始化时允许不带身份，作为一台“空白设备”运行；
2. 初始化以后如果创建身份，设备即与此身份绑定；
3. 如果想把多台设备组织在一起，可以共享身份：用一台不带身份的设备，向自己已绑定身份的设备发起共享申请，后者批准后，这些设备便以同一身份对外。

绑定一旦建立，无法简单解除——只能重置设备数据，让它变回一台“新设备”重新开始。

重置时数据会怎样，取决于平台和版本：

- **Windows（原型版）**：地址簿和聊天记录保存在一个 SQLite 数据库文件（`.db`）中，身份等个人信息保存在同名的 TOML 文件里。重置前把这两个文件留在原地（或备份后放回），即可保留数据。
- **Android 原型版**：只能整体重置，地址簿和聊天记录会一起清空，且无法找回。
- **Android 原生版**：支持加密备份与导入——重置前先导出备份，初始化时再导入，地址簿、聊天记录和身份都能恢复。

备份与导入目前只有 Android 原生版支持。跨平台原型版以通用性和功能验证为目标，没有计划加入这个功能，所以在原型版上重置就意味着数据彻底丢失，请提前按上面 Windows 的说明自行保留文件。

备份是怎么工作的：**导出**时你需要设置一个密码，备份用这个密码加密后，默认保存到系统的下载目录；**导入**时通过系统选择备份文件，再输入同一个密码解密。密码不会上传到任何地方，开发者也拿不到解密的手段——这既是好事也有代价：别人拿不到可读的副本，但同时**没有找回密码的途径**，密码忘了，那份备份就打不开了。

<h3 class="faq-q">“节点”指什么？</h3>

每台运行 isle 的设备都是一个 p2p 网络节点，拥有一个设备唯一标识。所有通信都发生在节点之间；身份不只是节点对外展示的“名片”——它还包含签名与加密的密钥，用来确认消息的发送者和接收者都是可信的目标。

<h3 class="faq-q">原型版和 Android 原生版有什么区别？</h3>

两者都是客户端 <code>isle</code> 的构建版本，只是侧重点不同。

- **跨平台原型版** —— 也就是官网现在能下载到的版本。同一套代码分别打包成 Windows x64 与 Android arm64（Android 端运行在 WebView 里）。它的任务是尽可能广泛地验证点对点这条路走得通：一次覆盖多个平台，并且具备完整到能真正收发消息、拨打电话的功能。代价是需要深度依赖系统的部分最薄弱——后台保活、通知、规范表现的通话界面，整体稳定性一般。
- **Android 原生版** —— 即将完成，与原型版共用同一套底层。它直接面向 Android 开发，因此依赖系统支持的部分表现好得多（后台保活、通知、可与系统电话界面和锁屏协同的通话界面）。当前范围更窄：这一版还没有实现消息收发功能。发布渠道为 Google Play；两条线都会继续推进——原生版会补上消息收发，原型版也会采纳它确立的界面设计。

除特别说明外，本 FAQ 中提到 Android 的地方，指的都是跨平台原型版。

<h3 class="faq-group">使用</h3>

<h3 class="faq-q">两台设备不在同一个局域网，能联系上吗？</h3>

可以。跨网络时，连接通过 iroh 网络建立，当前阶段需要在应用的网络设置中开启“免费中继网络”。

目前使用 n0 提供的基础网络设施。随着 IPv6 可用性大大增强，实际使用中大多数场景可以直接建立点对点连接，很少需要经中继转发。国内可以正常使用。未来有必要时，会增加自建 relay / DNS 的方式。

<h3 class="faq-q">怎么让 Android 版保持在线？</h3>

没有服务器暂存消息，所以受话端必须保持 isle 在线。不同系统做法不同：

- **通用做法**：插电；在系统设置中把屏幕超时调到最长（或常亮）；保持 isle 在前台。
- **MIUI / 小米**：打开最近任务（应用切换）列表，长按 isle 的卡片，选择“锁定”，防止被一键清理。
- **其他国产 ROM**：大多有类似机制——在电池 / 省电设置里对 isle 关闭电池优化，允许自启动和后台运行。

各家 ROM 行为差异很大。如果你在自己机型上发现了有效做法，欢迎写信告诉我，我会补充到这里。

<h3 class="faq-q">换手机或重装后，数据能迁移吗？</h3>

短时间内不能。协议尚未成熟，数据库 / 配置文件格式仍在变化，导入导出功能还没有实现（见首页[已知限制]({{ '/' | relative_url }}#limits)）。换机意味着从一台“新设备”重新开始。

<h3 class="faq-q">版本升级后旧数据还能用吗？</h3>

不保证。协议尚未成熟，数据库与配置文件格式仍在变化：

- 版本号**前两位**变化，基本意味着数据格式不兼容，旧数据无法继续使用；
- **第三位**变化，一般不涉及数据库格式变化，旧数据理论上可用。

<h3 class="faq-group">安全与隐私</h3>

<h3 class="faq-q">谁能看到我的消息？</h3>

只有消息的发送者和接收者。

消息通过 iroh 的点对点连接直接传输，采用自定义协议，默认享有基于 QUIC 的端到端加密。即使流量偶尔经由中继转发，中继看到的也只是加密数据。

将来如果增加服务器节点暂存消息：点对点消息会使用接收端的身份密钥加密，保证只有接收端能够解密，服务器只是“看不到内容的保管箱”。

<h3 class="faq-q">密钥保存在哪里？丢了会怎样？</h3>

isle 没有账号系统，也不需要登录——配置与设备绑定，压根不存在“密码”，自然也谈不上“找回密码”。密钥保存在设备本地，不上传任何地方：

- **Android**：保存在应用的专属目录中。
- **Windows**：保存在应用所在的同一目录下。

isle 中有两类密钥，丢失的后果不同：

- **设备密钥**：设备初始化时生成，是这台设备在网络中的标识。丢失后无法找回，只能重置数据，作为一台新设备重新开始。
- **身份密钥**：创建身份时生成。丢失后，如果还有其他设备共用这一身份，可以通过上面说的共享流程取回；如果所有共用设备都丢失了，就只能创建一个新身份。

需要留意的是：旧身份的公钥仍留在联系人的地址簿里，创建新身份后，只能通过带外方式（当面、电话等）告知联系人更新。也就是说，保证“身份和设备可信”的责任在用户自己——没有服务器替你担保，这正是 direct-chat 与传统即时通信的根本区别之一。

<h3 class="faq-group">计划与其他</h3>

<h3 class="faq-q">“辅助节点”是什么？这不就又变回服务器了吗？</h3>

某种意义上是的——服务器本质上就是一台 24 小时开机的电脑。它的价值在于：服务器存在时，即使目标设备不在线，消息也有地方暂存，“双方必须同时在线”的限制就被打破。

目前的考虑是**允许自建服务器**：基础设施由用户自己掌握，消息的存储也就仍然在用户自己手里。

服务器节点还可以承担群聊支持，但这种情况下服务器会持有群聊密钥，服务器管理员将能够查看群聊消息。这是未来设计中的权衡，不是现在的功能。

<h3 class="faq-q">和 Signal / Tox / Briar 有什么区别？</h3>

- **Signal**：成熟、好用，但需要通过手机号注册账号，且依赖官方运营的服务器。
- **Tox**：技术上思路相近。Tox 依赖 DHT 进行节点查找；direct-chat 基于 iroh 网络，DHT 只是设备查找的其中一种方式，整体更灵活、更自主——iroh 的基础网络设施同样可以自建替换。
- **Briar**：强调设备之间通过多种途径互传（Tor、局域网、蓝牙等），面向更极端的通信环境。direct-chat 相对简单直接。
- direct-chat 的核心是把**数据自主权还给用户**：无需理解复杂概念，就能简单地使用并自建私有的通信网络。

<h3 class="faq-q">为什么不开源？</h3>

技术可行性还需要验证，精力也有限——过早开源会带来不必要的噪音和干扰。这不影响交流：问题和建议都很欢迎。

<h3 class="faq-q">支持 iOS / macOS / Linux 吗？</h3>

原型在技术上可以编译出这几个平台的版本，但目前没有精力去验证和解决各平台的问题，暂时不做处理。

<h3 class="faq-q">发现了 bug，怎么反馈？</h3>

附上系统版本、应用版本（例如 v{{ site.app_version }}）和复现步骤，写信到 <cliff@clifftop.win>，或到 [GitHub 仓库](https://github.com/CliffHan/direct-chat-release)提 issue。

</div>
</section>
